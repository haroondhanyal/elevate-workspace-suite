import { useState } from 'react'

function CreateProject() { const [name, setName] = useState(''); const [type, setType] = useState('Software development'); const [created, setCreated] = useState(false); function submit(event) { event.preventDefault(); if (name.trim()) { localStorage.setItem('rh98-project', JSON.stringify({ name, type })); setCreated(true) } } return <main className="project-create-page"><header className="workspace-header"><a className="jira-logo" href="/jira"><span>✦</span> RH98 <small>JIRA</small></a><a className="back-link" href="/jira">← Back to dashboard</a></header><section className="project-create-card"><p className="jira-kicker">New project</p><h1>Create a project</h1><p className="heading-note">Set up a project space for your team and start shipping.</p><form onSubmit={submit}><label>Project name<input value={name} onChange={(event) => setName(event.target.value)} placeholder="e.g. RH98 Brand Platform" autoFocus required /></label><label>Project key<input className="key-input" placeholder="RH98" maxLength="6" /></label><label>Project template<select value={type} onChange={(event) => setType(event.target.value)}><option>Software development</option><option>Marketing campaign</option><option>Product discovery</option></select></label><div className="template-preview"><span>▥</span><div><strong>Scrum</strong><p>Plan work in sprints with a flexible board and backlog.</p></div><b>Selected</b></div><div className="project-create-actions"><a href="/jira">Cancel</a><button className="jira-primary" type="submit">Create project →</button></div>{created && <p className="success-message">Project created successfully. <a href="/jira/board">Open project board</a></p>}</form></section></main> }
export default CreateProject
