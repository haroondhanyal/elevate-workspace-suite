import { useState } from 'react'

function JiraCustomize() { const [saved, setSaved] = useState(false); const [accent, setAccent] = useState('#70409a'); return <main className="customize-page"><header className="workspace-header"><a className="jira-logo" href="/jira"><span>✦</span> RH98 <small>JIRA</small></a><a className="back-link" href="/jira">← Back to dashboard</a></header><div className="customize-layout"><aside className="settings-nav"><p className="jira-kicker">Project setup</p><h2>Customize</h2><a className="settings-active" href="/jira/customize">Project details</a><a href="/jira/board">Board & columns</a><a href="#workflows">Workflows</a><a href="#roles">People & roles</a></aside><section className="customize-card"><p className="jira-kicker">RH98 Brand Platform</p><h1>Customize your project</h1><p className="heading-note">Make this project fit the way your team works.</p><form onSubmit={(event) => { event.preventDefault(); setSaved(true) }}><label>Project name<input defaultValue="RH98 Brand Platform" /></label><label>Project description<textarea defaultValue="Build and launch the RH98 Brand experience." rows="4" /></label><label>Project color<div className="color-picker"><input type="color" value={accent} onChange={(event) => setAccent(event.target.value)} /><span>{accent}</span></div></label><div className="customize-actions"><a href="/jira">Cancel</a><button className="jira-primary" type="submit">Save changes →</button></div>{saved && <p className="success-message">Project settings saved.</p>}</form></section></div></main> }
export default JiraCustomize
